version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATA_DIR=/data
      - DATABASE_URL=sqlite:////data/parts.db
    volumes:
      - parts_data:/data
    restart: unless-stopped

volumes:
  parts_data: